# 개발 일지

---

## 2026-06-17 (1일차) - 프로젝트 시작

### 한 일
- 프로젝트 기획 확정 (로컬 AI + 인트라넷 + 핸드헬드)
- 부품 결정 및 디바이스마트 주문
- GitHub 저장소 생성
- 노션 작업 일지 세팅
- 노트북에 Ollama + Qwen3 8B 설치 및 테스트

### 주요 결정
- 본체: 라즈베리파이 5 8GB 정품 선택
- 액세서리: 디바이스마트 묶음 구매 (가성비 우선)
- 모델: Qwen3 8B 우선, 3B 병행 운영 예정

---

## 2026-06-18 (2일차) - README 정리 및 차별화 컨셉 업로드

### 한 일
- README.md 갱신 (블루투스 키보드 "선택, 필수 아님" 명시, 노트북 사전 테스트 체크 완료로 갱신)
- 차별화 컨셉 문서 업로드 (GitHub `docs/프로젝트_차별화_컨셉.md` 신규 생성, Notion에도 동일 페이지 신규 생성)

### 주요 결정
- 프로젝트 차별화 방향: Cyberdeck → Privacy-First Pocket Server (프라이버시 / AI 중심 제어 / 듀얼 모드 3축 정체성)
- 소형화 목표: 스마트폰 크기(약 85×56mm) 지향 (이후 06-19에 "주머니 사이즈"로 재조정)
- 배터리 폼팩터 확정: 리튬폴리머 직결 (UPS HAT 제외)
- 케이스 두께 기준: 액티브 쿨러 높이 측정 후 설정 완료
- 디스플레이 방식·적층 구조는 미확정 상태로 다음 날(06-19) 확정

---

## 2026-06-19 (3일차) - 라즈베리파이 설치 완료 및 컨셉 업데이트

### 한 일
- 라즈베리파이 OS 설치, SSH 접속 완료
- Ollama + Qwen3 8B 설치 완료
- 전체 설계 컨셉 정교화
- 인트라넷 정체성 재정의 (서버 → 자료 이동 통로)
- 적층 구조 확정 (디스플레이-배터리-보드-쿨러, 약 42mm)
- 디스플레이 확정 (DSI 3.5인치 터치스크린)
- 배터리 확정 (LiPo 2000mAh + IP5306)

### 주요 결정
- 케이스 없이 1차 완성 (방열 최적 + 빠른 동작 확인)
- 3B 기본 + 8B 필요시 수동 전환 전략
- Open WebUI 풀스크린 부팅 자동 실행

---

## 2026-06-20 (4일차) - 인트라넷 + 로컬 AI 환경 완성

### 한 일
- 성능 실측 (Qwen3 8B)
- Tailscale 설치 및 인트라넷 구성
- Docker 설치
- Open WebUI 설치 및 Ollama 연결
- 폰 Tailscale 설치 + Open WebUI 접속 확인

### 성능 실측 결과
| 테스트 | eval rate | 온도 |
|---|---|---|
| 짧은 질문 | 2.26~2.29 tokens/s | 60도 |
| 중간 질문 | 2.22 tokens/s | 60도 |
| 긴 질문 | 2.10 tokens/s | 64.2도 |

RAM 점유: 8B 모델 6.3GB / 7.9GB → 3B 기본 운영 전략 확정

### 트러블슈팅
- Qwen3 8B Thinking 모드 끄기 실패 (`/no_think`, `/set parameter think false` 무효)
- Open WebUI Ollama URL 수정 필요 (`host.docker.internal` → `localhost`)

### 현재 네트워크 구성
```
폰 + 노트북 (Tailscale)
        ↓
Tailscale 인트라넷 (100.103.109.7)
        ↓
라즈베리파이 5
├── Ollama (Qwen3 8B)
└── Open WebUI (:8080)
```

---

## 2026-06-21 (5일차) - 1차 필수 항목 전체 완료

### 한 일
- 태블릿(Galaxy Tab S11) Tailscale 설치 및 라즈베리파이 연결 확인
- Llama 3.2 3B 설치 (`ollama pull llama3.2:3b`)
- `~/.bashrc`에 alias 등록 (`ai` → Llama 3.2 3B, `ai-smart` → Qwen3 8B)
- SSH 키 인증 설정 (ed25519 키 생성, `authorized_keys` 등록)
- 부팅 시 Open WebUI 자동 실행 설정 (systemd + Docker 재시작 정책)
- Syncthing 파일 동기화 설치 및 설정 (라즈베리파이 + 노트북, 외부 릴레이/전역 발견 비활성화)
- 1차 필수 항목 10개 전체 완료 ✅

### 주요 결정
- Llama 3.2 3B 한국어 입력 시 다국어 혼용 확인 → 한국어 작업 시 영어 입력 권장
- alias 용도 분리: `ai`(일상/간단, 빠름) vs `ai-smart`(복잡한 추론·한국어)
- Syncthing 외부 노출 최소화: `relaysEnabled`/`globalAnnounceEnabled` 모두 false, NAT 통과 비활성화

### 트러블슈팅
- Llama 3.2 3B 한국어 입력 시 다국어 혼용 → 영어 입력으로 우회 확인

### 다음에 할 일
- [ ] 거치 서버 모드 (이더넷 트리거 자동 전환)
- [ ] DSI 터치스크린 + LiPo 배터리 하드웨어 통합
- [ ] RGB LED 장착 (GPIO 연동)
- [ ] 킬 스위치 1차 (rfkill + GPIO)
- [ ] 3D 프린팅 케이스 (Fusion 360)

---

## 2026-06-22~24 (6~8일차) - 하드웨어 설계 확정 및 부품 구매 완료

### 한 일
- 적층 구조 재설계: 기존 42mm → 36mm로 축소 (목표 4cm 이내 충족)
- 배터리 시스템 사양 변경: LiPo 2000mAh + IP5306 → LiPo 3000mAh + IP5305T
- 배선 연결 구조 확정: 배터리 → 충방전 모듈 → USB-C female 소켓 PCB → 라즈베리파이 DSI 커넥터 → DSI 케이블(S자 고정) → 터치스크린
- 납땜 포인트 2곳 확정: ① 배터리 전선 → 충방전 모듈 Battery +/-, ② 충방전 모듈 Output +/- → USB-C female 소켓 PCB V+/GND
- 케이스 설계 방향 확정: 휴대 방향 가로(닌텐도 스위치형), 3D 프린팅(Fusion 360 또는 OnShape), 측면 통풍구 확보, DSI 케이블 S자 고정(최소 5mm 여유)
- 부품 구매 완료 (아래 표)
- Llama 3.2 3B 운용 가이드 확정 (영어 입력 시 품질 양호, 한국어는 품질 낮음)
- Open WebUI 모델 운용 전략 확정 (8B 응답 완료 후 5분 자동 언로드 또는 수동 전환)

### 구매 완료 부품
| 부품 | 사양 | 구매처 |
|---|---|---|
| Waveshare 3.5인치 DSI LCD | 800×480, 정전식, DSI | 알리 (Waveshare 공식) |
| LiPo 배터리 3000mAh | 605080, 3.7V, 6mm 두께 | 알리 |
| IP5305T 충방전 모듈 | USB-C 입력, 패스쓰루 지원 | 알리 |
| 충방전 모듈 여유분 | USB-C, 5V 2A | 알리 |
| USB-C female 소켓 PCB | 16P, 5.1K 저항 내장 | 알리 |
| 전선 26AWG | 10미터, 6색상 | 알리 |
| 납땜 인두기 세트 | 220V 60W, 온도조절, EU플러그 | 쿠팡 |

### 주요 결정
- 적층 구조를 42mm → 36mm로 재설계 (목표 4cm 이내 충족). 기존 design-concept.md/Notion의 42mm·LiPo 2000mAh·IP5306 스펙은 본 항목 기준으로 정정함
- 커넥터 대신 납땜 직결 방식 유지, 납땜 포인트 2곳으로 확정
- 케이스 휴대 방향은 가로(닌텐도 스위치형)로 확정, 3D 프린팅 툴은 Fusion 360 또는 OnShape 중 선택
- Llama 3.2 3B는 영어 입력 기준으로 운용(한국어는 품질 낮음), 8B와 동시 실행 금지

### 트러블슈팅
- Qwen3 8B와 Llama 3.2 3B 동시 실행 시 RAM 초과로 SSH 연결 끊김 발생 → 모델 전환 시 반드시 `ollama stop <모델명>`으로 이전 모델 종료 후 전환
- Open WebUI 운영: 8B 응답 완료 후 5분 자동 언로드 대기, 또는 수동 `ollama stop qwen3:8b` 후 3B 사용

### 다음에 할 일
- [ ] DSI 터치스크린 + LiPo 배터리 + 충방전 모듈 실제 조립/납땜
- [ ] 거치 서버 모드 (이더넷 트리거 자동 전환)
- [ ] RGB LED 장착 (GPIO 연동)


---

## 2026-06-25 (9일차) - 케이스 설계 확정 및 킬스위치/LED 계획

### 한 일
- 하드웨어 동작 원리 학습 완료
- 케이스 설계 확정 (2층 배치, 포트 배치)
- 킬 스위치 + RGB LED 방식 확정
- 추가 부품 구매 완료/취소

### 학습 내용
- 전원 계통 흐름: 배터리 → IP5305T → USB-C female PCB → Pi5
- IP5305T 역할 및 파워패스 개념
- LiPo 배터리 규격 읽는 법 (605080)
- DSI vs HDMI 차이
- 부품별 단자 입출력 정리

### 주요 결정
- 케이스 2층 배치: IP5305T + USB-C female PCB를 배터리 옆에 수직 배치, U자 어댑터로 Pi5 직결
- 배터리·Pi5 영역 밖으로 삐져나오는 부분까지 케이스로 감쌈
- microSD: 내부 고정, 외부 노출 안 함
- 위쪽 긴 면: micro HDMI ×2, USB-C 전원 노출 / 아래쪽 긴 면: 이더넷, USB-A ×4 노출
- 충전 포트: IP5305T 아래쪽 외부 노출
- 킬 스위치/RGB LED: 측면 공간 비워둠 (부품 도착 후 확정), 전원 버튼은 Pi5 내장 버튼 활용
- 킬 스위치 방식: rfkill 소프트웨어 확정. Wi-Fi ON=파랑, OFF=빨강
- ⚠️ 빨간색이 기존 온도 위험(80도+), 저전압 경고와도 겹쳐 세 가지 의미가 중복됨. 구분 방법은 제시된 자료만으로는 알 수 없어 정하지 않음

### LED 개수 분리 확정 (추가 결정)
- 위 빨간색 중복 문제를 LED 3개 분리로 해결: RGB LED ×1 → RGB LED ×3
- LED1: 무선 상태 (파랑=ON, 빨강=OFF, 녹색=거치모드+외부접속중)
- LED2: 온도 경고 (주황=70~80도, 빨강=80도+)
- LED3: 저전압 경고 (빨강). 평상시(정상 전압) 색상은 자료에 정의되어 있지 않아 미정
- 물리적으로 분리되어 위치로 구분 가능하므로 기존 색상 중복 문제 해소
- ⚠️ 부품 수량 정정: RGB LED 1개 → 3개. 저항은 LED 1개당 R/G/B 채널별 1개가 일반적인 배선 방식이라 3개 → 9개로 정정 (이 9개는 제시된 자료에 없는 일반적인 전자공학 가정이며, 실제 사용할 LED 규격에 따라 달라질 수 있음)

### 부품 구매
| 부품 | 사양 | 구매처 | 상태 |
|---|---|---|---|
| U자형 USB-C to USB-C 어댑터 | 180도, PD 65W | 알리 | ✅ 구매 완료 (배송 6/30~7/5) |
| USB-C to USB-C 90도 꺾임 케이블 | 15cm | 알리 | ❌ 구매 취소 |

### 다음에 할 일
- [ ] 토글 스위치 1개, RGB LED 3개, 저항 9개 구매
- [ ] 부품 도착 후 킬스위치/LED Python 스크립트 작성
- [ ] U자 어댑터 도착 후 조립 (6/30~7/5 예정)
- [ ] DSI 터치스크린 + LiPo 배터리 + 충방전 모듈 실제 조립/납땜

---

## 2026-07-22~23 (하드웨어 검증) - 배터리 플랜 A 안정성 확인

### 한 일
- 플랜 A(LiPo 605080 + FM5324HJ1) 환경에서 Qwen 2.5 3B, Gemma 4 E2B 모두
  수분간 안정 구동 실측 (스로틀링 `0x0` 완전 유지 확인)

### 주요 결정
- 배터리 플랜 A 99% 확정 (플랜 B/C는 대안으로 구매만 완료, 실측 보류)

> ⚠️ 이 시기 하드웨어 조립·부품 세부 작업 기록은 Notion 작업 일지에 더
> 상세히 남아있습니다. 이 파일은 GitHub 동기화 공백을 메우기 위한 요약입니다.

---

## 2026-07-27 (소프트웨어 재점검) - 모델 운영 전략 재정리

### 한 일
- 휴대 모드 모델을 Gemma 4 E2B, 거치 모드를 Qwen3 8B로 재정리
- `detect-power-mode` systemd 서비스, 스마트 큐잉(`ai-que`), killon/killoff 재검증 완료

---

## 2026-07-30 (하드웨어 인계 시점) - 조립 상태 정리

### 한 일
- Pi5 + 액티브 쿨러 + Waveshare DSI 터치스크린 물리 조립 완료
- 배터리 내부 배치(디스플레이-배터리-Pi5) 확정, 절연 처리는 미결정으로 남김
- 케이스 설계(Fusion 360)는 `bottom:1` 컴포넌트 스케치 단계에서 중단

### 다음에 할 일
- [ ] 케이스 설계 재개
- [ ] 배터리 진단(충전 중 다운 현상) 필요 시 진행

---

## 2026-08-25~28 (중간발표 피드백 대응) - AI/소프트웨어 계층 전면 재설계

중간발표에서 "기성품 조합에 불과함", "모델 선정 이유 불명확"이라는 지적을
받고, AI/소프트웨어 계층 전체를 재검토했습니다. 상세 내용은 분량이 많아
별도 문서로 분리했습니다 → [`2026-08-ai-layer-redesign.md`](./2026-08-ai-layer-redesign.md)

### 요약
- AI 역할을 범용 채팅형 비서 단일 역할로 단순화 (검토했던 "프라이버시
  감시자" 기능은 폐기)
- 보안·기능 소프트웨어 12개 채택 (설계 확정, 구현은 진행 중)
- RAG 설계 확정: nomic-embed-text-v2, Open WebUI Knowledge Base, 1시간
  간격 증분 동기화
- Modelfile 시스템 프롬프트·파라미터 튜닝 초안 작성
- **Gemma 4 E2B 반복 OOM 트러블슈팅** — 실제 원인은 vision/audio
  capability 상시 점유 + Pi5 자체 화면에서 브라우저를 띄운 채 개발 작업을
  병행한 테스트 환경 문제였음이 밝혀짐
- **휴대 모드 모델을 Gemma 4 E2B → Qwen3 4B로 교체 결정**
- GitHub 저장소 SSH 최초 연동 (계정·저장소명이 `hyeok-ju0/raspi-local-ai-`
  임을 확인 — 하이픈 표기 포함)

---

## 2026-08-31 - thinking 모드 비활성화 시도 및 트러블슈팅

상세 내용은 별도 문서 참고 → [`2026-08-31-thinking-mode-troubleshooting.md`](./2026-08-31-thinking-mode-troubleshooting.md)

### 요약
- Open WebUI UI의 think 토글이 작동하지 않음을 확인 (boolean/string 타입
  혼동 버그로 추정)
- 비관리자 계정의 API 키 발급을 위해 그룹 기본 권한에 "API Keys" 권한을
  별도로 활성화해야 함을 확인, 발급 완료
- curl로 Open WebUI API에 `think:false`를 직접 전송하는 테스트 중 Pi5가
  응답 없이 멈추는 사고 발생 → 자연 복구됨, 파일시스템 손상 없음, 단 OOM
  킬러 흔적이 없어 기존 OOM 사고들과는 다른 원인으로 추정(미확정)
- 영구 저널(persistent journal) 활성화 — 다음부터는 재부팅 후에도 로그 보존됨

> ⚠️ **이 시점까지 GitHub 저장소의 README, configs/setup.md,
> docs/design-concept.md, docs/프로젝트_차별화_컨셉.md는 실제 진행 상황보다
> 몇 달 뒤처져 있었음이 뒤늦게 발견되어(2026-09-01), 전체적으로 재작성했습니다.**
## 2026-09-02 - CPU 폭주 재현·확인, RAG 아키텍처 방향 검토 (외출 중 원격 작업)

> ⚠️ 이 항목은 원래 누락되어 있었다가 2026-09-09에 뒤늦게 발견되어 추가함.
> 실제 작업일은 2026-09-02이며, 이전에 실수로 09-07 항목에 잘못 포함되어 있었음.

### 한 일
- 외출 중이라 직접적인 하드웨어 작업은 어려운 상황이었으나, Termux SSH로 원격
  진단은 진행함
- 8/31 "Pi5 응답없음" 사고 재조사: `top`으로 실시간 관찰한 결과 Ollama 프로세스가
  **최대 396~399%(Pi5 4코어 사실상 전체 점유)**까지 치솟는 것을 직접 재현·확인
- 8/31 스크린샷에 남아있던 "Wait, ... Wait, ..." 반복 사고 루프가 원인으로 최종
  확인됨 — 결론에 도달하지 못하고 토큰을 계속 생성하며 CPU를 독점, OOM 킬러가
  개입할 이유가 없어 그때 로그에 OOM 흔적이 전혀 없었던 이유도 설명됨
- 안전장치(`--max-time`, `num_predict` 제한)를 건 재현 테스트 진행, 타임아웃
  120초에도 0바이트 응답으로 확인 — Open WebUI API가 `options`/`num_predict`
  파라미터를 최상위가 아닌 잘못된 위치에 있으면 무시한다는 사실 확인
- 클라이언트(curl)가 타임아웃돼도 Ollama 서버 쪽 생성은 자동으로 취소되지 않는다는
  것을 확인(`ollama ps`에 여전히 실행 중으로 표시됨) → `ollama stop`으로 수동 정리
- RAG 아키텍처 방향(A: Open WebUI API / B: Ollama 직통 / C: 하이브리드) 검토 시작,
  Open WebUI의 검색 전용 API 부재를 확인하며 방향 재검토 필요성 인지
- RGB LED 장착 계획 폐기, 거치대 제작은 품평회 이후로 일정 조정 확인(하드웨어
  트랙, 구두 확인만 되고 별도 문서에는 미반영 상태로 남아있었음)

### 다음에 할 일
- [ ] GitHub 문서 5개(8/28~9/2 준비분) 반영 — 이후 09-07에 완료됨
- [ ] 감시 스크립트(watchdog) 작성 — 이후 09-07에 완료됨
- [ ] RAG 아키텍처 최종 결정 — 이후 09-09에 완료됨

---


---

## 2026-09-07 - Syncthing 근본 해결, 감시 스크립트 제작, 휴대 모드 모델 최종 확정

상세 내용은 별도 문서 참고 → [`2026-09-07-syncthing-watchdog-model-final.md`](./2026-09-07-syncthing-watchdog-model-final.md)

### 요약
- Syncthing "연결 끊김" 문제의 근본 원인(폴더 공유 클러스터 설정 불일치) 발견 및 해결
  — 이후 파일 전송이 다시 정상화됨
- 지연 반영되어 있던 GitHub 문서 5개(README, setup.md, design-concept.md,
  프로젝트_차별화_컨셉.md, devlog 추가분) 반영 완료
- (8/31 사고의 CPU 폭주 재현·원인 확인 상세는 9/2 항목 참고)
- **감시 스크립트(`~/ai-safe.sh`) 신규 제작**: Ollama 네이티브 API(11434) 직통,
  240초 타임아웃 + 자동 정리, 대화형 모드, 대화 저장/재개(`ai new`/`ai list`/
  `ai resume`)까지 지원하는 통합 스크립트로 완성
- **휴대 모드 모델 재탐색 끝에 최종 확정**: Qwen3 4B(기본 빌드, thinking 안 꺼짐) →
  Phi-4-mini(한국어 사실 오류 심각) → Qwen3.5 4B(vision+thinking 둘 다 포함되어
  제외) → **`qwen3:4b-instruct`**(thinking 기본 꺼짐, 정확한 한국어 확인) 순으로
  검토 후 채택
- `ai` alias를 `~/ai-safe.sh` 기반으로 갱신

---

## 2026-09-09 - RAG 파이프라인 완성, 성능 버그 수정, 데이터 암호화

상세 내용은 별도 문서 참고 → [`2026-09-09-rag-pipeline-and-commands.md`](./2026-09-09-rag-pipeline-and-commands.md), 명령어 전체 목록은 [`commands-reference.md`](./commands-reference.md)

### 요약
- RAG 아키텍처를 Open WebUI와 완전히 독립된 자체 파이프라인으로 최종 결정
  (Open WebUI API가 세 차례 신뢰성 문제를 보였기 때문)
- RAG 파이프라인 구축: `rag_index.py`(인덱싱, PDF/MD/TXT 지원, 증분 처리) /
  `rag_search.py`(코사인 유사도 검색) / `rag_context.py`(임계값 0.35 필터링)
- 실제 전공·철학 수업자료(487개 청크)로 실측: 관련 질문 유사도 0.5~0.72,
  무관 질문 0.15~0.17로 뚜렷하게 구분됨을 확인
- 트러블슈팅 2건: 임베딩 배치 크기(512토큰) 초과로 대부분 청크 실패 → 청크
  크기 1000→400자로 조정해 해결 / 대화 기록에 RAG 컨텍스트가 영구 누적되는
  버그(원본 질문과 전송본을 분리해 해결) → 대화가 길어질수록 느려지던 원인이었음
- 모델 유지 정책 개선: 정상 종료 시엔 모델 유지, 타임아웃일 때만 강제 정리 +
  `keep_alive: 30m`으로 재로딩 지연 최소화
- **RAG on/off 토글 기능 도입**: 매번 자동 적용 시 프롬프트가 길어져 응답이
  느려지는(참고자료 없으면 4초, 있으면 약 40초) 문제를 겪은 뒤, `ai`(OFF 기본)/
  `ai-rag`(ON)/대화 중 `rag on`·`rag off`로 전환
- `ai-smart`(거치 모드)를 `ai-safe.sh` 패턴으로 통일, `ai-smart-rag` 추가
- 거치 모드 Open WebUI think:false 재검증 완료(Qwen3 8B 정상), `qwen3:4b-instruct`
  Modelfile 템플릿·stop 토큰 검증 완료(정상)
- **RAG 데이터 gocryptfs 암호화 마이그레이션 완료**: `~/.rag_data_encrypted`에
  암호화 저장, `~/rag_data`는 마운트 지점으로 유지(스크립트 수정 불필요).
  `rag-unlock`/`rag-lock`/`rag-status` alias로 관리. 마이그레이션 전후 검색
  결과 동일함을 확인해 정상 작동 검증
- 부수 발견: 트레이딩 봇 사이드 프로젝트가 2개→3개로 확장됨, 거치 모드 실행 시
  메모리 여유가 110MB까지 떨어지는 것 확인(당장 위험은 아니나 계속 추적 필요)
- 늘어난 명령어·alias를 `docs/commands-reference.md`로 통합 정리
- 테스트하다 방치된 모델 7개 정리(`qwen3.5:4b`, `phi4-mini`, `qwen3:4b`,
  `ai:latest`, `gemma4:e2b`, `qwen2.5:3b`, `qwen3:8b-nothink`), 디스크 약 29.9GB 확보

> ⚠️ 9/7, 9/9 작업 당일에 이 요약 항목을 devlog.md에 추가하는 걸 빠뜨렸다가
> 뒤늦게(9/9) 발견해 추가함. 상세 문서는 각 날짜에 바로 커밋됐었음.
