# 2026-09-09 작업 기록: RAG 파이프라인 완성, 성능 버그 수정, 데이터 암호화

## 1. RAG 아키텍처 최종 결정
Open WebUI API가 지금까지 세 번 연속(UI 토글 무시, 파라미터 무시, 원인불명 hang)
신뢰할 수 없는 모습을 보여, RAG를 **Open WebUI와 완전히 독립된 자체 파이프라인**으로
구축하기로 결정. 거치 모드의 Open WebUI 화면 자체는 그대로 유지(브라우저 기반
원격 접속용), RAG 검색·생성만 별도 경로로 처리.

## 2. RAG 파이프라인 구축
새로 만든 스크립트 3개(`~/rag_data/` 안):
- `rag_index.py` — notes/coursework 폴더를 스캔해 청크 분할 + nomic-embed-text-v2-moe로
  임베딩. PDF(pypdf)/마크다운/텍스트 지원. 파일 해시 기반 증분 처리(변경 없는 파일은
  재임베딩 안 함)
- `rag_search.py` — 질문을 임베딩해 저장된 청크와 코사인 유사도 비교, top-k 반환
- `rag_context.py` — 검색 + 임계값(0.35) 필터링 + LLM에 넣을 컨텍스트 포맷팅을
  한 번에 처리하는 헬퍼

### 실측 데이터
실제 전공 수업자료(포인터/배열 등, PDF 10개, 487개 청크)와 철학 수업자료(비트겐슈타인
논리철학논고)로 테스트:
- 관련 질문("포인터가 뭐야?", "배열과 포인터의 관계는?") → 유사도 0.5~0.72
- 무관한 질문("오늘 저녁 뭐 먹지") → 유사도 0.15~0.17
- 격차가 뚜렷해 임계값 0.35로 명확히 구분 가능함을 확인

### 트러블슈팅: 임베딩 배치 크기 초과
초기 청크 크기(1000자)로 인덱싱 시 대부분의 청크가 `input (533 tokens) is too
large to process. increase the physical batch size (current batch size: 512)`
오류로 실패. Ollama 임베딩 서버의 기본 배치 크기(512토큰) 제한에 걸린 것.
청크 크기를 1000→400자, 중첩을 100→50자로 조정해 해결 (전체 재인덱싱 후
487개 청크 전부 성공).

## 3. `ai-safe.sh`에 RAG 통합
- 자동 검색 후 임계값 이상일 때만 프롬프트에 참고자료로 삽입
- 참고자료가 실제로 쓰였을 때만 `📎 참고: [출처]` 형태로 화면에 표시(중간 지점 방식 —
  매번 자동 적용하되 언제 개입했는지는 투명하게 보여줌)

### 트러블슈팅: 대화 기록에 RAG 컨텍스트가 영구 누적되는 버그
RAG로 붙인 컨텍스트(최대 1200자 이상)를 원래 질문 파일에 덮어쓰는 실수로, 매 턴마다
그 내용이 그대로 대화 기록에 저장됨. 대화가 길어질수록 프롬프트가 눈덩이처럼
불어나 응답 속도가 계속 느려지는 원인이 됨. 원본 질문(`user_msg.txt`)과 RAG 붙인
전송본(`sent_msg.txt`)을 별도 파일로 분리해, 기록에는 항상 원본만 남도록 수정.

### 트러블슈팅: 매 턴 모델 재로딩
정상 종료 시에도 매번 `ollama stop`을 호출하던 기존 로직 때문에, 대화가 이어질
때마다 모델(3.5GB)을 처음부터 재로딩. 정상 종료 시에는 모델을 유지하고, 감시
스크립트가 실제로 타임아웃을 발동시켰을 때만 강제 정리하도록 수정. 추가로 요청에
`keep_alive: 30m`을 명시해 기본 5분보다 오래 유지되도록 함.

### 남은 지연 요인 (버그 아님, 하드웨어 한계)
RAG 컨텍스트가 포함되면 참고자료 없는 질문(4초) 대비 훨씬 오래(약 40초) 걸림.
Pi5는 CPU 전용이라 프롬프트가 길어지면 답변 생성 전 처리(prefill) 자체가 무거워짐.
버그가 아니라 속도와 검색 품질의 트레이드오프로 판단, 아래 4번 토글 기능으로 대응.

## 4. RAG on/off 토글 기능 도입
매번 자동 적용 대신, 필요할 때만 켜는 방식으로 전환:
- `ai` — RAG 꺼짐(기본, 빠름)
- `ai-rag` — RAG 켜짐
- 대화 중 `rag on` / `rag off` / `rag status`로 즉시 전환
- 사소한 버그: 입력 앞뒤 공백이 있으면 명령어 인식 실패 → 자동 트리밍 추가로 해결

## 5. `ai-smart`(거치 모드) 정리
`/usr/local/bin/ai-smart-safe`의 `ollama run qwen3:8b` 직접 호출 부분을
`ai-safe.sh`로 교체. 휴대 모드에서의 질문 큐잉 동작(전력 부족 시 질문 저장)은
그대로 유지. `ai-smart-rag` alias 추가.

## 6. 거치 모드 Open WebUI think:false 재검증
Qwen3 8B에 대해 Open WebUI 관리자 패널의 "think (Ollama)" UI 토글을 테스트한
결과 **정상 작동** — 사고 과정 없이 바로 답변. (8/31에 문제였던 건 Qwen3 4B
한정이었고, 8B는 이번이 최초 검증. 거치 모드는 Open WebUI 그대로 유지 확정)

## 7. `qwen3:4b-instruct` Modelfile 검증
- 템플릿: `<|im_start|>`/`<|im_end|>` 기반 정상 템플릿 확인 (Gemma4처럼 고장난
  `{{ .Prompt }}` 단일 템플릿 아님)
- stop 토큰: `"<|im_start|>"`, `"<|im_end|>"` 모델 자체 기본값이 정확함을 확인,
  별도 지정 불필요

## 8. RAG 데이터 gocryptfs 암호화 마이그레이션
- `gocryptfs`는 Debian Trixie 공식 저장소에 포함되어 있어 `apt install`로 설치
- 암호화 저장소: `~/.rag_data_encrypted/` (실제 암호화된 데이터가 저장되는 곳)
- 마운트 지점: `~/rag_data/` (기존 경로 그대로 유지 — 마운트하면 이 경로에서
  평문처럼 보이므로 스크립트 수정 불필요)
- 평문 데이터를 암호화 마운트 안으로 복사 후 검증 → 마이그레이션 전과 동일한
  검색 결과(유사도 0.590/0.519/0.502) 확인, 성공
- `rag-unlock`(마운트) / `rag-lock`(마운트 해제) / `rag-status`(상태 확인) alias 추가
- 부팅 시 자동 마운트 아님 — 매번 패스프레이즈 직접 입력 필요(기존 "기본은 차단,
  의도적으로만 연결" 원칙과 일치)

## 9. 부수적으로 발견한 것
`ps aux` 확인 중 트레이딩 봇 사이드 프로젝트가 2개(`igv`, `smh`)에서
**3개(`breakout`, `bear`, `bull`)로 확장**된 것을 발견. 거치 모드에서 `qwen3:8b`
실행 시 여유 메모리가 110MB까지 떨어지고 스왑(652MB)까지 쓰는 것을 확인 —
당장 위험하진 않으나(모델은 5분 후 자동 언로드), AI 프로젝트 메모리 예산 계산 시
계속 추적이 필요한 항목으로 재확인됨.

## 명령어 정리
오늘까지 늘어난 모든 alias·스크립트를 `docs/commands-reference.md`로 별도 정리함.

## 다음에 할 일
- [ ] 휴대 모드 화면/세션 잠금 (RAG 암호화만으로는 "기기가 켜진 채로 분실"되는
  시나리오를 못 막음 — 반드시 같이 구현해야 하는 짝)
- [ ] 로그 자동 삭제/만료 관리
- [ ] 클립보드 자동 초기화
- [ ] USB 데이터 유출 차단, 로컬 접근 감사 로그, 오프라인 비밀번호 관리자,
  설정 파일 무결성 검사
- [ ] 개인 위키/노트 에디터, 오프라인 사전 조회기, 로컬 일정 관리
- [ ] 트레이딩 봇 확장(3개)이 AI 메모리 예산에 미치는 영향 재점검
